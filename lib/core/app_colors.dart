import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0C4060);
  static const Color glow = Color(0xFFA7EBF2);
  static const Color text = Colors.white;
  static const Color error = Colors.redAccent;
}
